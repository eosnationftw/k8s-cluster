apiVersion: apps/v1
kind: Deployment
metadata:
  name: substreams-tier2
spec:
  selector:
    matchLabels:
      app: substreams-tier2
  replicas: 1
  template:
    metadata:
      labels:
        app: substreams-tier2
    spec:
      containers:
        - name: substreams-tier2
          image: fireeth
          args:
            - "start"
            - "-c"
            - "/config/config.yaml"
          ports:
            - name: grpc
              containerPort: 13045
              protocol: TCP
          volumeMounts:
            - name: config-volume
              mountPath: /config
      volumes:
        - name: config-volume
          configMap:
            name: substreams-tier2-config