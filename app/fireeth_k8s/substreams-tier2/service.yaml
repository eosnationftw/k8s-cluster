apiVersion: v1
kind: Service
metadata:
  name: substreams-tier2
spec:
  type: ClusterIP
  selector:
    app: substreams-tier2
  ports:
    - name: grpc
      port: 13045
      protocol: TCP
      targetPort: grpc
